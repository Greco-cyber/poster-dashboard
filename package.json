{
  "name": "poster-shift-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "GRECO | Одностраничник для данных по смене (фронт) + прокси к Poster (бэк).",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "start:server": "node server.js",
    "dev": "concurrently \"npm run start:server\" \"npm start\"",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "lucide-react": "^0.263.1",
    "node-fetch": "^2.7.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "proxy": "http://localhost:3001",
  "engines": {
    "node": ">=16"
  }
}
